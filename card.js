class Card {
  constructor(matchInfo, url) {
    this.matchInfo = matchInfo;
    this.matched = false;
    this.url = url;
  }
  match() {

    // this.matched= true;
  }
}


console.log('test-card')
