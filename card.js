class Card {
  constructor(matchInfo) {
    // need to match other same pic, 1,2,3,4,5 or A,B,C,D,E
    this.matchInfo = matchInfo;
    this.matched = false;
  }
  match() {

    // this.matched= true;
  }
}
